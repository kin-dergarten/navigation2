# Metrics as a measure for the localization accuracy

std_msgs/Header header

float64 total_sensor_model_score # total  unnormalized weight of all particles
float64 max_weight # weight of most likely particle cluster
int64 number_of_particles
